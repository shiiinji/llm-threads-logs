# Copy this file to `.env` and fill in values.
#
# Note: the binaries do NOT automatically load `.env`.
# Use one of:
# - `set -a; source .env; set +a` (bash/zsh)
# - direnv (`.envrc` + `direnv allow`)
#
# Obsidian vault root directory (required)
OBSIDIAN_VAULT="/Users/YOURNAME/Obsidian/MyVault"
#
# Folder name under the vault (required)
OBSIDIAN_AI_ROOT="AI Logs"
